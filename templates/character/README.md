# character template

Copy to `shows/<show_id>/characters/<char_id>/` then fill:

```
characters/<char_id>/
  profile.yaml
  views/ref_portrait.png
  views/front.png
  views/side.png
  views/back.png
  sheets/turnaround.png
  voice/profile.yaml
  voice/sample.wav   # optional
```
